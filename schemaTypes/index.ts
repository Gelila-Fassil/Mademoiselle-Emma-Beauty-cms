

import product from './product'
import category from './category'
import homePageShowcase from './home-pageShowcase'
import ceoProfile from './ceo'

// This array is what your sanity.config.ts file imports as 'schemaTypes'
export const schemaTypes = [
  product,
  category,
  homePageShowcase,
  ceoProfile,

  // If you had any schemas already here (like block content or page), keep them!
]